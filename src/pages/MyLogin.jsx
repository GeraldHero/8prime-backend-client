import { Button, Typography } from '@mui/material';
import React from 'react';

const MyLogin = () => {
  return (
    <div>
      <Button color='myColor'> MyLogin</Button>
    </div>
  );
};

export default MyLogin;
